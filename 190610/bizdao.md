```sql
CREATE TABLE T_FACTORY(
FACTNO VARCHAR2(5),
FACNAME VARCHAR2(14),
FACLOC VARCHAR2(13),
PRIMARY KEY(FACTNO)
)
desc t_PRODUCTS


CREATE TABLE T_PRODUCTS(
PDNO NUMBER, 
PDNAME VARCHAR2(10),
PDSUBNAME VARCHAR2(10),
FACTNO VARCHAR2(5),
PDDATE DATE,
PDCOST NUMBER,
PDPRICE NUMBER,
PDAMOUNT NUMBER,
PRIMARY KEY(PDNO),
CONSTRAINTS FK_PD FOREIGN KEY(FACTNO)
REFERENCES T_FACTORY(FACTNO)
);

INSERT INTO T_PRODUCTS VALUES(
002, 'COFFEE', 'LATTE', 2, SYSDATE, 1000, 2500, 100
)

INSERT INTO T_FACTORY (FACTNO, FACNAME, FACLOC) 
VALUES ('2', 'STARBUCKS', 'NEWYORK');

```

